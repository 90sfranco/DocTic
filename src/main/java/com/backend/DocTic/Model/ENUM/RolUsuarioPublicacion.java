package com.backend.DocTic.Model.ENUM;


/**
 * Enum RolUsuarioPublicacion (12/11/2024): Representa el rol de un usuario en relación a una publicación, con dos posibles valores: 
 * `autor` para indicar que el usuario es el creador principal de la publicación y `coautor` para indicar que el usuario 
 * contribuyó como colaborador.
 */

public enum RolUsuarioPublicacion {
    autor, coautor;
}
