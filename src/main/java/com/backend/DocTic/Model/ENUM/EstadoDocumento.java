package com.backend.DocTic.Model.ENUM;


/**
 * Enum EstadoDocumento (12/11/2024): Representa el estado de visibilidad de un documento, con dos posibles valores: 
 * `publico` para indicar que el documento es accesible públicamente y `privado` para indicar que el documento 
 * tiene acceso restringido.
 */

public enum EstadoDocumento {
    publico, privado
}
